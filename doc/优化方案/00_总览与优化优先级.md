# z-biz-tool-shared 优化方案 · 00 总览与优化优先级

> 状态：设计方案，未实现；基线 `fbda8d6`（z-biz-tool-shared 独立仓库 HEAD，工作区干净，无未提交改动）；当前事实 = 静态代码证据，指标 = 拟定验收目标。
> 本次仅新增 `doc/优化方案/` 下 7 份文档，未改动任何业务代码/配置/锁文件。

---

## 一、这个库的独特价值

z-biz-tool-shared 是 z-biz-tool 系列 10 个桌面应用（db / file / note / knowledge / terminal / sys / box / aigen / pet / agent-worker）唯一的跨项目共享代码库，承载四类本应收敛的能力：

1. **AI 能力中台**（`src/ai/`）：统一 AIConfig / executeAI 入口，避免每个应用各写一套 OpenAI 客户端；
2. **Agent 交互套件**（`src/agent/`）：AgentPanel / SQLEditor / QueryHistoryViewer 等可复用面板；
3. **主题 / 快捷键 / 通知三大横切系统**（`src/theme/`、`src/shortcuts/`、`src/notifications/`）；
4. **UI 组件与设计令牌**（`src/components/`、`src/shared/`、`src/styles/`）。

它的健康度直接决定 10 个下游项目的重复代码量与体验一致性——**库的价值以"下游真实 import"计量，而非以代码行数计量**。

## 二、一句话现状（基线事实）

**共享机制事实上已失效**：10 个下游项目全部通过 `file:../z-biz-tool-shared` 符号链接声明了依赖，但全仓库范围内只有 1 处真实 import（`z-biz-tool-file/src/agent/AgentPanel.tsx:17`）；9 个项目各自复制了一份完全相同的 6 文件 `src/agent/` 目录；`z-biz-tool-db/src/App.tsx:49` 留有注释"使用本地 Agent 组件（临时方案，待共享库修复后迁移到 z-biz-tool-shared）"——下游明确在等这个库修复。详细证据见 [01_现状审计与问题清单](./01_现状审计与问题清单.md)。

根因是三重的：

- **分发形态缺失**：`package.json` 无 build 脚本、无 `exports`，`main`/`types` 直指 `src/index.ts` 源码，`dist/` 仅含 tsbuildinfo（`tsconfig` 同时设 `noEmit:true` 与 `declaration/composite:true`，配置自相矛盾）；
- **文档与现实脱节**：INTEGRATION_GUIDE 宣称的 `new AIManager(...)`、`AIManagerProvider`、`useAIContext` 等 API 在源码中不存在；DEV_GUIDE / examples 使用的子路径 `z-biz-tool-shared/ai` 按当前包结构无法解析；IMPLEMENTATION_SUMMARY 宣称"terminal/aigen 集成 ✅、测试 ✅"，实际两个项目零引用、仓库零测试；
- **API 面与设计系统内部重复冲突**：两套主题系统（`src/theme/` 十主题 vs `src/shared/ThemeContext` 明暗两态）、三套设计令牌、两套快捷键体系，顶层 `src/index.ts` 靠 `export *` + 手工白名单规避重名冲突。

## 三、优化优先级总表

| 级别 | 主题 | 核心动作 | 详情 |
|------|------|----------|------|
| **P0-1** | 建立真实分发能力 | tsup 构建 ESM+d.ts、`exports` 子路径映射、`files` 白名单；react/antd/zustand 移入 peerDependencies，删除幽灵依赖 immer/codemirror | [03 §3/§4/§5](./03_架构与工程化优化.md) |
| **P0-2** | 文档与现实对齐 | 重写 INTEGRATION_GUIDE（删除不存在的 API）、修复 examples 导入路径并纳入 typecheck、修正 IMPLEMENTATION_SUMMARY 虚假完成声明 | [02 §5/§6](./02_API设计与开发者体验.md) |
| **P0-3** | 公共 API 面收敛 | 顶层显式导出清单 + API 报告文件（api-extractor 理念）锁定导出面；为 `EmptyState`、`ThemeProvider` 等重名符号定唯一出口 | [02 §2/§3](./02_API设计与开发者体验.md) |
| **P0-4** | 打通第一个真实下游 | 以 z-biz-tool-file 为试点：删除其复制的 `src/agent/`，改为 `z-biz-tool-shared/agent` 子路径导入，跑通 typecheck+build | [05 阶段4](./05_实施路线与任务拆解.md) |
| **P1-1** | 双主题/双令牌/双快捷键归一 | theme 与 shared/ThemeContext 合并为单一主题模块；三套 designTokens 收敛为一套；shortcutManager 与 useKeyboardShortcuts 打通 | [03 §2](./03_架构与工程化优化.md) |
| **P1-2** | mock/stub 显性化 | AgentManager 4 个 TODO mock、InteractionFeedback 空函数、ContextManager.loadFromFile 空实现：要么注入真实实现、要么从公共 API 面移除并标注 `@experimental` | [02 §4](./02_API设计与开发者体验.md) |
| **P1-3** | 版本与破坏性变更治理 | 语义化版本策略、file-link"提交即发布"风险管控、弃用周期与迁移指南模板 | [04 §1/§2/§3](./04_稳定性兼容与发布安全.md) |
| **P1-4** | 类型质量 | 开启 `strict`、消除 8 处 `any`、AIConfig/ThemeName 运行时校验、错误类型契约 | [02 §4](./02_API设计与开发者体验.md) |
| **P2-1** | AI 客户端能力补全 | timeout/AbortController 落地（`AIConfig.timeout` 已声明未使用）、provider 工厂真实分支、密钥不落前端明文 | [01 A-11](./01_现状审计与问题清单.md)、[04 §5](./04_稳定性兼容与发布安全.md) |
| **P2-2** | CI 与回归防线 | 库自身 typecheck/lint/test/build 流水线 + 下游 10 项目集成回归矩阵 | [06 §2/§3](./06_测试验收与风险清单.md) |
| **P2-3** | 死代码与体验细节 | ThemeSelector 死代码修复、toast id 生成与定时器治理、`localStorage` 键命名空间统一 | [01 A-8/A-12](./01_现状审计与问题清单.md) |

**立即要做的 P0**：P0-1（构建与 exports）是一切的前置——没有可解析的包入口，文档修复和下游回归都无从验证。

## 四、分阶段能力演进（详见 [05_实施路线与任务拆解](./05_实施路线与任务拆解.md)）

- **阶段 1（可构建的库）**：产物、exports、peer 依赖、files 白名单落地；验收 = `dist/` 有 ESM+d.ts 且下游 vite 可解析全部子路径。
- **阶段 2（可信的文档）**：四份 GUIDE 按"声明 vs 实现"逐条校对重写；examples 可编译；验收 = 文档中每个 import 语句都能在包内解析。
- **阶段 3（收敛的 API）**：重名符号归一、mock 显性化、strict 模式、API 报告锁定；验收 = 导出面 100% 出现在 API 报告且无未声明重名。
- **阶段 4（被真实使用）**：file 试点 → db 移除"临时方案"注释 → 其余项目按需迁移，删除 9 份复制的 `src/agent/`；验收 = 下游真实 import 数 ≥ 9 处、复制目录清零。
- **阶段 5（被治理的库）**：CI、语义化发布、下游回归矩阵、bundle 体积预算；验收 = 见 [06](./06_测试验收与风险清单.md) 量化指标。

## 五、文档索引

| 文档 | 内容 |
|------|------|
| [01_现状审计与问题清单](./01_现状审计与问题清单.md) | 基线事实、下游集成现状表、14 项已确认缺陷（含文件/行号证据）、待验证假设、已存在能力 |
| [02_API设计与开发者体验](./02_API设计与开发者体验.md) | 公共 API 面收敛方案、类型契约、错误约定、文档与 examples 质量重建 |
| [03_架构与工程化优化](./03_架构与工程化优化.md) | 模块边界归一（明确推荐）、tsup 构建与产物格式、依赖治理、tree-shaking、下游集成方式推荐 |
| [04_稳定性兼容与发布安全](./04_稳定性兼容与发布安全.md) | 语义化版本、file-link 同步风险、破坏性变更与迁移流程、下游兼容矩阵、凭据与供应链安全 |
| [05_实施路线与任务拆解](./05_实施路线与任务拆解.md) | 5 阶段任务编号、文件级改造点、前置依赖、角色与资源假设、兼容回滚（仅规划，不执行） |
| [06_测试验收与风险清单](./06_测试验收与风险清单.md) | 单元/集成/API 契约/发布/性能验收矩阵、16 项量化验收指标、下游回归清单、风险登记册 |

## 六、核心原则（全套方案共用）

1. **API 面向下游稳定**：导出面即契约，任何新增/删除/改名都走 API 报告 diff 审查；
2. **破坏性变更必须有迁移路径**：弃用标注 → 迁移指南条目 → 至少一个 minor 周期缓冲 → major 删除；
3. **凭据不入包**：`apiKey` 等敏感配置只允许运行时注入，发布产物与文档示例中不得出现真实密钥形态字符串；
4. **类型契约完整**：公共 API 无 `any`、无未导出的关联类型，下游 `tsc --noEmit` 可独立通过。
