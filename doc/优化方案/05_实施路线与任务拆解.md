# z-biz-tool-shared 优化方案 · 05 实施路线与任务拆解

> 状态：设计方案，未实现；基线 `fbda8d6`；当前事实 = 静态代码证据，指标 = 拟定验收目标。
> 本篇为**规划排期，全部任务均未执行**。每个任务标注：编号、涉及文件级改造点、前置依赖、角色、资源假设、兼容与回滚。改造点中"新增/修改"文件路径均为规划，链接只连已存在源码（`../../src/...`）与本套 docs。

---

## 零、角色与资源假设

| 角色 | 职责 | 假设投入 |
|------|------|----------|
| 库维护者（Lib Owner） | 构建/API/依赖/发布，阶段 1-3 主力 | 1 人 |
| 前端集成（Integrator） | 下游试点迁移与回归，阶段 4 主力 | 1 人（可兼） |
| 评审（Reviewer） | API 报告 diff、破坏性变更审批 | 按需 |

资源假设：① 单人主导、 sprint（约 1 周）为节奏单位；② 无 CI 预算限制，使用现有 GitHub Actions（下游已有 `.github/workflows/build.yml`，可复用模式）；③ 不引入付费服务；④ 所有命令由维护者在本地/CI 执行，**本方案不代为执行任何 shell/构建/发布**。工期为粗估，非承诺。

---

## 一、阶段 1：可构建的库（P0，工期约 1 sprint）

**目标**：让库产出 ESM+CJS+d.ts，`exports` 可被下游解析，依赖形态正确。对应 [03 §2/§3/§4](./03_架构与工程化优化.md)、[01 A-1/A-2](./01_现状审计与问题清单.md)。

| 任务 | 文件级改造点 | 前置 | 角色 | 兼容/回滚 |
|------|-------------|------|------|-----------|
| **T1.1** 引入 tsup 构建 | 新增 `tsup.config.ts`（[03 §2.1](./03_架构与工程化优化.md) 示例）；`../../package.json` scripts 加 `build`/`dev`/`clean` | 无 | Lib Owner | 纯新增，回滚=删配置文件+还原 scripts |
| **T1.2** 修正 tsconfig | 修改 `../../tsconfig.json`：移除 `composite`/`declaration`/`declarationMap`/`outDir`，保留 `noEmit:true` 供 typecheck（[03 §2.3](./03_架构与工程化优化.md)） | T1.1 | Lib Owner | 回滚=还原 tsconfig（git 可追溯） |
| **T1.3** 依赖重分类 | 修改 `../../package.json`：react/antd/zustand/@ant-design/icons → peerDependencies（宽范围）；antd/icons/codemirror → optional peer；移除幽灵依赖 immer + 2 codemirror；tsup/vitest/@testing-library 入 devDependencies（[03 §3.1/§3.2](./03_架构与工程化优化.md)，幽灵依赖证据见 [03 §3.2](./03_架构与工程化优化.md)） | 无 | Lib Owner | 回滚=还原 package.json + package-lock；需重新 `npm install` |
| **T1.4** exports + files + type:module | 修改 `../../package.json`：加 `exports`（9 子路径）、`main`/`module`/`types` 指向 dist、`files:["dist"]`、`sideEffects`（[03 §4.1](./03_架构与工程化优化.md)） | T1.1 | Lib Owner | 回滚=还原 package.json |
| **T1.5** 建 tokens 子路径骨架 | 新增 `src/tokens/index.ts`（先 re-export 现有三套令牌之一，[02 §4](./02_API设计与开发者体验.md)），保证 T1.4 的 `./tokens` entry 有实体 | T1.1 | Lib Owner | 新增文件，回滚=删除 |

**阶段 1 验收**：`npm run build` 产出 `dist/*.js`+`*.cjs`+`*.d.ts`（当前 dist 仅 tsbuildinfo，[01 A-1](./01_现状审计与问题清单.md)）；下游能 `import ... from 'z-biz-tool-shared/ai'` 解析成功。详见 [06 §1](./06_测试验收与风险清单.md)。

---

## 二、阶段 2：可信的文档（P0，工期约 1 sprint）

**目标**：四份 GUIDE + examples 与源码一致，示例可编译。对应 [02 §6](./02_API设计与开发者体验.md)、[01 A-6/A-7](./01_现状审计与问题清单.md)。

| 任务 | 文件级改造点 | 前置 | 角色 | 兼容/回滚 |
|------|-------------|------|------|-----------|
| **T2.1** 重写 INTEGRATION_GUIDE | 修改 `../../INTEGRATION_GUIDE.md`：删除 `AIManager` class/`AIManagerProvider`/`useAIContext`/`generateText` 等不存在 API，替换为真实 `useAIManager`（[`../../src/ai/AIManager.ts:147`](../../src/ai/AIManager.ts)）；导入示例改子路径 | T1.4 | Lib Owner | 文档改动，git 可回滚 |
| **T2.2** 修正 IMPLEMENTATION_SUMMARY | 修改 `../../IMPLEMENTATION_SUMMARY.md`：terminal/aigen"集成 ✅"改为"未集成（0 引用）"；"测试 ✅"改为"无测试"；或整体标注为历史归档 | 无 | Lib Owner | 文档改动 |
| **T2.3** 修正 DEV_GUIDE / MIGRATION_GUIDE | `../../DEV_GUIDE.md` 移除 `npm run dev` 运行示例的错误指引（无该脚本）；`../../MIGRATION_GUIDE.md` 把"已完成迁移"改为"待执行剧本"（[04 §3.3](./04_稳定性兼容与发布安全.md)） | 无 | Lib Owner | 文档改动 |
| **T2.4** 修复 examples 导入 | 修改 `../../examples/*.tsx`（5 文件）：`z-biz-tool-shared/ai` 等子路径与 exports 对齐；顶部加"演示 API + 预期"注释 | T1.4 | Lib Owner | git 回滚 |
| **T2.5** examples 纳入 typecheck | 新增 `tsconfig.examples.json`（include examples），`../../package.json` 加 `typecheck:examples` 脚本 | T2.4 | Lib Owner | 新增文件 |
| **T2.6** README 精简 + 子路径表 | 修改 `../../README.md`：加"快速开始 + 子路径导入表 + 指向 doc/优化方案" | T1.4 | Lib Owner | 文档改动 |

**阶段 2 验收**：examples typecheck 0 error；文档中每条 import 均可解析（[06 §1](./06_测试验收与风险清单.md)）。

---

## 三、阶段 3：收敛的 API（P0/P1，工期约 2 sprint，含破坏性变更）

**目标**：重名符号归一、模块边界重组、mock 显性化、strict、API 报告锁定。对应 [02 §2/§5](./02_API设计与开发者体验.md)、[03 §1/§6](./03_架构与工程化优化.md)。**本阶段含破坏性变更，须走 [04 §3.2](./04_稳定性兼容与发布安全.md) 四步迁移。**

| 任务 | 文件级改造点 | 前置 | 角色 | 兼容/回滚 |
|------|-------------|------|------|-----------|
| **T3.1** 主题系统归一 | 合并 `../../src/theme/*` 与 `../../src/shared/ThemeContext.tsx`：`useTheme` 唯一化，过渡期兼容返回 `{mode,toggle,themeColors,setTheme,currentTheme}`（[02 §2.3](./02_API设计与开发者体验.md)、[04 §3.1](./04_稳定性兼容与发布安全.md)）；修复 ThemeSelector 死代码（[01 A-8](./01_现状审计与问题清单.md)）；localStorage 键统一命名空间（[01 A-14](./01_现状审计与问题清单.md)） | 阶段1 | Lib Owner | **破坏性**：旧 shared `useTheme` 标 @deprecated；回滚需 git revert + 通知下游 |
| **T3.2** 令牌归一 | `src/tokens/index.ts` 落地唯一令牌；`../../src/styles/designTokens.ts`+`../../src/shared/designTokens.ts`+`../../src/components/UIDesignSystem.tsx` 的 DESIGN_TOKENS 改为 @deprecated re-export（[02 §4](./02_API设计与开发者体验.md)） | T1.5 | Lib Owner | **破坏性**：旧名过渡一个 minor |
| **T3.3** 快捷键归一 | 合并 `../../src/shortcuts/*` + `../../src/shared/useKeyboardShortcuts.tsx` + `../../src/components/GlobalShortcutHandler.tsx` 到 shortcuts 子路径（[03 §1.2](./03_架构与工程化优化.md)） | 阶段1 | Lib Owner | 旧路径 re-export 过渡 |
| **T3.4** 取消 shared 目录 | `../../src/shared/*` 内容分流：布局组件→components，主题→theme，令牌→tokens，快捷键→shortcuts；`../../src/index.ts` 去 `export *` 改显式（[02 §2.2](./02_API设计与开发者体验.md)） | T3.1/T3.2/T3.3 | Lib Owner | **破坏性**：shared 深路径 re-export 过渡 |
| **T3.5** mock 显性化 + AgentService 注入 | `../../src/agent/AgentManager.ts` 引入 `AgentService` 接口与 `configureAgentService`（[02 §5](./02_API设计与开发者体验.md)）；`../../src/interactions/interactionPatterns.ts` 的 `InteractionFeedback` 移出根入口；stub 加 `@experimental`/`@mock` | 阶段1 | Lib Owner | 新增注入点向后兼容；InteractionFeedback 移除实测 0 引用 |
| **T3.6** 消除公共 any + 导出 store 类型 | 修改 [01 A-13](./01_现状审计与问题清单.md) 列出的 3 处公共签名 any；export `AIStore`/`AgentStore` 等接口（[02 §3.1/§3.2](./02_API设计与开发者体验.md)） | 阶段1 | Lib Owner | 类型收紧可能暴露下游错误，需回归 |
| **T3.7** strict 分阶段开启 | 修改 `../../tsconfig.json`：`strictNullChecks`→`noImplicitAny`→`strict:true`（[03 §6.2](./03_架构与工程化优化.md)），逐步修错 | T3.6 | Lib Owner | 每步以 typecheck 绿为门槛，可停在任一子阶段 |
| **T3.8** API 报告锁定 | 引入 api-extractor（或 tsup dts 汇总），新增 `api-report/*.api.md`，CI diff（[03 §6.3](./03_架构与工程化优化.md)） | T3.4 | Lib Owner | 新增文件 |

**阶段 3 验收**：重名符号 0 组；公共 API any=0；导出面 100% 在 API 报告；strict=true 且 typecheck 绿（[06 §1](./06_测试验收与风险清单.md)）。

---

## 四、阶段 4：被真实使用（P0，工期约 2 sprint）

**目标**：打通下游，删除 9 份复制的 `src/agent/`。对应 [00 P0-4](./00_总览与优化优先级.md)、[01 §二](./01_现状审计与问题清单.md)。

| 任务 | 文件级改造点（下游仓库，非本库） | 前置 | 角色 | 兼容/回滚 |
|------|-------------------------------|------|------|-----------|
| **T4.1** file 试点迁移 | `z-biz-tool-file/src/agent/AgentPanel.tsx:17` 深路径 import 改 `z-biz-tool-shared/agent`；删除 file 复制的 `src/agent/`（6 文件）；跑通 file `npm run typecheck`+`build` | 阶段1-3 | Integrator | 回滚=恢复 file 本地 agent 目录（git） |
| **T4.2** db 去"临时方案" | `z-biz-tool-db/src/App.tsx:49` 移除本地 AgentPanel/useAgentStore，改 import 库；删 db 复制的 `src/agent/`；移除 `db/vite.config.ts:10` 指向 src 的 alias，改指 dist 或依赖 exports | T4.1 验证通过 | Integrator | 回滚=git revert |
| **T4.3** 其余 7 项目批量迁移 | note/knowledge/terminal/sys/box/aigen/pet/agent-worker 各删复制 `src/agent/`，改库导入；terminal/aigen 的独立 aiStore 按需接 `useAIManager`（[01 A-6](./01_现状审计与问题清单.md)） | T4.2 | Integrator | 逐项目 PR，单项目回滚不影响其余 |
| **T4.4** workspace 化（可选终态） | 根 `package.json` 加 `workspaces:["z-biz-tool-*"]`；下游依赖从 `file:` 改 workspace 协议（[03 §5 方式B](./03_架构与工程化优化.md)） | T4.1 | Lib Owner+Integrator | 回滚=还原 file: 引用 |

**阶段 4 验收**：下游真实 import ≥9 处；复制 `src/agent/` 目录清零；下游 10/10 typecheck 通过（[06 §3](./06_测试验收与风险清单.md)）。

---

## 五、阶段 5：被治理的库（P1/P2，工期约 1-2 sprint）

**目标**：CI、语义化发布、下游回归矩阵、bundle 预算、能力补全。对应 [04](./04_稳定性兼容与发布安全.md)、[06](./06_测试验收与风险清单.md)。

| 任务 | 文件级改造点 | 前置 | 角色 | 兼容/回滚 |
|------|-------------|------|------|-----------|
| **T5.1** CI 流水线 | 新增 `.github/workflows/ci.yml`：typecheck+lint+build+test+API diff+secret 扫描+npm audit（[04 §5.3/§5.4](./04_稳定性兼容与发布安全.md)） | 阶段1-3 | Lib Owner | 新增文件 |
| **T5.2** 下游回归 job | CI 增矩阵 job：对 10 下游跑 typecheck（[06 §3](./06_测试验收与风险清单.md)） | T5.1, T4.x | Lib Owner | 新增 |
| **T5.3** 版本发布流程 | 引入 changesets，新增 `.changeset/`、发布脚本；README 标注 0.x 破坏性约定（[04 §1.2](./04_稳定性兼容与发布安全.md)） | T5.1 | Lib Owner | 新增 |
| **T5.4** 单元/集成测试 | 新增 `src/**/*.test.ts(x)` + vitest 配置：AIManager/theme/shortcuts/notifications/tokens（[06 §1](./06_测试验收与风险清单.md)） | 阶段3 | Lib Owner | 新增 |
| **T5.5** bundle 体积预算 | CI 加 size 检查（如 size-limit），对每个子路径设阈值（[06 §4](./06_测试验收与风险清单.md)） | T1.1 | Lib Owner | 新增 |
| **T5.6** AI 能力补全（P2） | `../../src/ai/AIManager.ts`：落地 timeout/AbortController（[01 A-10](./01_现状审计与问题清单.md)）、provider 工厂真实分支或明确单一 provider（[01 A-11](./01_现状审计与问题清单.md)）；审计 console 不泄漏 apiKey（[04 §5.2](./04_稳定性兼容与发布安全.md)） | 阶段3 | Lib Owner | 行为增强，向后兼容 |
| **T5.7** 通知健壮性（P2） | `../../src/notifications/notificationManager.ts`：id 用递增计数器/`crypto.randomUUID`，定时器句柄管理，clearToasts 清定时器（[01 A-12](./01_现状审计与问题清单.md)） | 阶段3 | Lib Owner | PATCH 级修复 |

**阶段 5 验收**：CI 全绿门禁生效；测试覆盖率达标；bundle 在预算内；发布 SOP 可复现（[06](./06_测试验收与风险清单.md)）。

---

## 六、任务依赖拓扑（关键路径）

```
T1.1 ─┬─ T1.2
      ├─ T1.4 ─── T2.1/T2.4/T2.6 ── (阶段2 文档)
      ├─ T1.5 ─── T3.2
T1.3 ─┘
阶段1 ── T3.1 ─┐
        T3.3 ─┼─ T3.4 ── T3.8(API报告)
        T3.5 ─┘
        T3.6 ── T3.7(strict)
阶段3 ── T4.1 ── T4.2 ── T4.3 ── (T4.4 可选)
阶段1-3 ── T5.1 ── T5.2/T5.3
阶段3 ── T5.4/T5.6/T5.7 ; T1.1 ── T5.5
```

**关键路径**：T1.1 → T1.4 → T3.4 → T3.8 → T4.1 → T4.2 → T5.2。构建（T1.1）是一切前置；模块归一（T3.4）是下游迁移（T4.x）前置；下游打通后才能建回归门禁（T5.2）。

## 七、跨阶段兼容与回滚总原则

1. **每任务独立可回滚**：优先纯新增（配置文件/新目录），修改类靠 git 精确 revert；
2. **破坏性变更集中在阶段 3**，且严格走 [04 §3.2](./04_稳定性兼容与发布安全.md) 四步（@deprecated → MIGRATION 条目 → 缓冲 → MAJOR 移除）；
3. **下游迁移（阶段 4）逐项目 PR**，单项目失败不阻塞其余，db/file 保留本地 agent 副本直至对应迁移 PR 合入；
4. **本方案文档（doc/优化方案）与代码改造解耦**：文档已落地，代码任务全部待执行，不因文档存在而声称任何改造已完成。

> 全篇任务 **T1.1–T5.7 共 30 项（阶段1×5 + 阶段2×6 + 阶段3×8 + 阶段4×4 + 阶段5×7），均为规划，未执行**。验收标准见 [06](./06_测试验收与风险清单.md)。
